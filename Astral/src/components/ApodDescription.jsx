import React from "react";

const ApodDescription = ({ title }) => {
  return (
    <div>
      <h1 className="font-bold text-[3vw] bg-red-700">
        {" "}
        i am not run why ? {title}
      </h1>
    </div>
  );
};

export default ApodDescription;
